<layer-list 
	xmlns:android="http://schemas.android.com/apk/res/android">
    <item 
		android:right="0dp" 
		android:left="0dp" 
		android:top="0dp" 
		android:bottom="0dp">
        <shape>
            <solid 
				android:color="@color/me_background_front" />
            <stroke
                android:width="4dp"
                android:color="@color/me_background_back" /> <!-- Ganti warna sesuai kebutuhan -->
            <corners 
				android:radius="50dp" />
            <size 
				android:height="30dp" />
        </shape>
    </item>
</layer-list>